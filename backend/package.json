{
  "name": "canadawill-ingest-root",
  "private": true,
  "engines": { "node": ">=20 <21" },
  "scripts": {
    "postinstall": "npm ci --omit=dev --prefix express-ingest",
    "start": "node express-ingest/standalone.js"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "@azure/storage-blob": "^12.17.0"
  }
}
