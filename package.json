{
  "name": "canadawill-ingest",
  "version": "1.0.0",
  "description": "CanadaWill Ingest API with sentiment analysis - force deploy",
  "main": "server.js",
  "scripts": {
    "start": "bash -lc 'cd /home/site/wwwroot && if [ -f node_modules.tar.gz ] && [ ! -d node_modules ]; then tar -xzf node_modules.tar.gz -C /home/site/wwwroot; fi; exec node server.js'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@azure/storage-blob": "^12.17.0",
    "openai": "^4.104.0",
    "@anthropic-ai/sdk": "^0.23.0"
  },
  "engines": {
    "node": "20.x"
  }
}
